<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/updateblog.css">
    <style>
        body {
    margin: 0;
    padding: 0;
    font-family: 'Arial', sans-serif;
}

div {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #333;
    padding: 10px;
    color: white;
    border: 1px solid red;
}

div p {
    margin: 0;
    font-size: 1.5rem;
    color: white;
    font-weight: bold;
    text-align: left;
}

nav {
    display: flex;
}

nav div {
    margin: 0 15px;
}

nav a {
    text-decoration: none;
    color: white;
    font-weight: bold;
}

/* Adjust styles for small screens */
@media (max-width: 768px) {
    div {
        flex-direction: column;
        align-items: flex-start;
    }

    nav {
        margin-top: 10px;
    }

    nav div {
        margin: 10px 0;
    }
}

    </style>
</head>

<body>
    {{>navbar}}
    <form action="/updateblog/{{blog.id}}" method="POST" style="margin-left: auto; margin-right:auto;">
        <label for="imgUrl">imgUrl</label>
        <input type="text" id="imgUrl" name="imgUrl" value="{{blog.imgUrl}}">
        <label for="title">title</label>
        <input type="text" name="title" id="title" value="{{blog.title}}">
        <label for="caption">caption</label>
        <input type="text" name="caption" id="caption" value="{{blog.caption}}">
        <label for="category">category</label>
        <select name="category" id="category">
            <option value="sports" {{#ifEquals blog.category 'sports' }}selected{{/ifEquals}}>Sports</option>
            <option value="business" {{#ifEquals blog.category 'business' }}selected{{/ifEquals}}>Business</option>
            <option value="educational" {{#ifEquals blog.category 'educational'}}selected{{/ifEquals}}>Educational</option>
            <option value="current affairs" {{#ifEquals blog.category 'current affairs' }}selected{{/ifEquals}}>Current Affairs
            </option>
        </select>
        <button type="submit">Submit</button>
    </form>
</body>

</html>